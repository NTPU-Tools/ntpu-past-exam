import CreatePostDialog from "@/containers/Dialogs/CreatePostDialog";
import UpdatePostStatusDialog from "@/containers/Dialogs/UpdatePostStatusDialog";

const Dialogs = () => (
  <>
    <CreatePostDialog />
    <UpdatePostStatusDialog />
  </>
);

export default Dialogs;
